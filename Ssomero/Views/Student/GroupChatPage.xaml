<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Ssomero.Views.Student.GroupChatPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:Ssomero.Models"
    BackgroundColor="#F1F5F9"
    Title="{Binding GroupName}">

    <Grid RowDefinitions="*,Auto">

        <CollectionView Grid.Row="0" ItemsSource="{Binding Messages}" Margin="0,8,0,0">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="6"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:GroupMessageDto">
                    <VerticalStackLayout Margin="16,0" Spacing="2">
                        <Label Text="{Binding SenderName}" FontSize="11"
                               TextColor="#5B21B6" FontAttributes="Bold"/>
                        <Border BackgroundColor="White" StrokeShape="RoundRectangle 10"
                                StrokeThickness="0" Padding="12,8">
                            <Label Text="{Binding Body}" FontSize="14" TextColor="#0F172A"
                                   LineBreakMode="WordWrap"/>
                        </Border>
                        <Label Text="{Binding SentAt, StringFormat='{0:HH:mm}'}"
                               FontSize="10" TextColor="#94A3B8" HorizontalOptions="End"/>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Border Grid.Row="1" BackgroundColor="White" StrokeThickness="0" Padding="12,8">
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                <Entry Grid.Column="0" Text="{Binding MessageText}"
                       Placeholder="Type a message..." FontSize="14"
                       TextColor="#0F172A" BackgroundColor="Transparent"/>
                <Button Grid.Column="1" Text="Send"
                        Command="{Binding SendCommand}"
                        BackgroundColor="#5B21B6" TextColor="White"
                        CornerRadius="8" Padding="16,0"/>
            </Grid>
        </Border>

    </Grid>
</ContentPage>
